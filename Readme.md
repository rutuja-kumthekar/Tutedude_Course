# Hello World - HTML

This is my first HTML Task(1).

## Description

A simple HTML page that displays "Hello World!" using the `<h1>` tag.

## Technologies Used

- HTML5

## What I Learned

- Basic HTML structure
- How to use the `<h1>` heading tag
- How to create an HTML file

## How to Run

1. Download or clone this repository.
2. Open the `index.html` file in any web browser.

## Code

```html
<h1>Hello World!</h1>


# CSS Selectors Task 

This task contains the complete solution for the "CSS Selectors" assignment. The project demonstrates the practical application of various CSS selectors, including class selectors, ID selectors, element selectors, descendant selectors, and structural pseudo-classes (`:nth-child`).

---

## Project Structure

```text
├── index.html    # Contains the HTML structure for headings, paragraphs, and lists
└── style.css     # External stylesheet containing all styling rules and selector implementations